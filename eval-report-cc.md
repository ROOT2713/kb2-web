# 知识库方案对比评估报告 — CC 评估版

**评估日期**：2026-07-07
**评估对象**：参考方案（本地知识库底座POC/MVP设计）vs kb2-web（知识库V2运行系统）

---

## 一、项目差异对比

### 1.1 架构层级对比

| 架构层级 | 参考方案（方案A） | kb2-web（方案B） | 差异分析 |
|---------|-----------------|-----------------|---------|
| **数据治理层** | 独立设计为第一层，含数据清洗、分级分类、脱敏规则、版本管理 | 内嵌于知识加工流程，**无独立治理子模块**；版本管理通过 active/superseded 标记实现 | 方案A将数据治理前置为独立层，方案B治理能力分散且未体系化 |
| **知识加工层** | 本地文档解析 → 切分 → 向量化流水线，强调"先治理后加工" | 文档解析 + Hindsight 向量引擎分块（**29,297 chunks**），含 concept 抽取（**3,809 concepts**）、KG 三元组（**45 triples**） | 方案B知识加工深度远超方案A的设计预期，已具备概念图谱和矛盾检测能力 |
| **存储索引层** | 预设 PostgreSQL + pgvector + FTS，关系型为主、向量为插件 | SQLite（**45MB**）+ Hindsight 内存引擎（**12 Bank**），纯文件型 + 自研向量引擎 | 存储选型差异显著：方案A偏生产级关系库，方案B以轻量文件库支持快速迭代 |
| **权限控制层** | 检索前过滤，规划细粒度文档/段落级权限 | JWT 双通道 + admin/viewer **两级角色**，前端 Vue role 感知 | 方案B已实现基本权限但粒度粗（仅2用户2角色），方案A规划更细但未落地 |
| **RAG服务层** | FastAPI + 本地/内网大模型 + 可选 Reranker | FastAPI + **DeepSeek v4 flash** + **5种 rerank 模式**（含 cross-encoder） | 方案B已上线多路召回和多种精排策略，方案A尚在规划阶段 |
| **审计运营层** | 日志埋点、性能监控、定期评估 | **66题 LLM-Judge** 测试集 + CI 每日回归 + T0/T1/T2 精度调优，但**缺少独立审计日志模块** | 方案B评估验证体系更成熟，但运营审计模块缺失 |

### 1.2 数据模型对比

| 维度 | 参考方案（A） | kb2-web（B） | 对比 |
|-----|-------------|-------------|-----|
| **文档模型** | 关系表结构，含文档元数据、版本、分类标签 | **303文档**（285 active + 18 superseded），parent_chunks 4,383 条 | 方案A侧重元数据完备性，方案B已落地版本追溯 |
| **向量存储** | pgvector 表，与关系数据同库 | Hindsight **12 Bank**，按领域分库（**11 Bank 多库体系**） | 方案B的多库隔离更灵活，但 pgvector 的事务一致性更强 |
| **知识图谱** | 未规划（首期明确不建议引入 Neo4j） | **45 kg_triples** + **3,809 concepts**（93.5% 有 summary）+ **158 synonym_map** | 方案B已超出方案A首期范围，构建了轻量知识图谱 |
| **矛盾管理** | 未涉及 | **2,029 concept_contradictions** | 方案B具备冲突检测能力，为测评场景中的标准矛盾识别提供了基础 |
| **缓存模型** | 未明确设计 | L1 内存缓存 + **query_cache 表**（4条）L2 持久化缓存 | 方案B实现了双层缓存，方案A未覆盖该领域 |
| **用户模型** | 规划多角色细粒度权限 | **2 users**（1 admin + 1 viewer），角色表结构简单 | 方案B模型极简，适合 POC 但无法支持生产环境组织架构 |

### 1.3 检索管线对比

| 环节 | 参考方案（A） | kb2-web（B） | 差距评估 |
|-----|-------------|-------------|---------|
| **查询预处理** | 未详述 | **查询分解** + **同义词扩展**（158条） + **金额层级展开** + **标准编号归一化** | 方案B已实现4种查询增强策略 |
| **召回通道** | 语义检索 + 全文检索（pgvector + FTS） | **三路召回**：Hindsight 语义 + BM25 关键词 + **取费表 SQL** | 方案B多了结构化数据召回通道 |
| **融合策略** | 未明确（推测加权或线性组合） | **RRF 融合**（k=60） | 方案B采用了学术验证的融合算法 |
| **精排策略** | 可选 Reranker（未实现） | **5种 rerank 模式**：default/multidim/confidence/freshness/**cross_encoder** | 方案B已上线 Cross-Encoder（~0.3s/query） |
| **业务优化** | 未涉及 | **C1-StdBoost**（doc_facts 按发布日期排序） + **D9 history anchor**（历史锚点注入） | 方案B有面向测评业务的专项优化 |
| **缓存加速** | 未涉及 | L1 + L2 双层缓存 | — |

**结论**：方案B的检索管线已进入深度优化阶段，召回-融合-重排链路完整且经业务调优；方案A的管线设计处于概念验证层面。

### 1.4 数据治理对比

| 治理维度 | 参考方案（A） | kb2-web（B） | 差距 |
|---------|-------------|-------------|-----|
| **数据清洗** | 规划为治理层核心 | 依赖文档解析阶段的基础处理，无独立清洗模块 | B 缺失 |
| **数据分级分类** | 明确规划 | **未实现** | B 缺失 |
| **脱敏处理** | 规划 | **未实现** | B 缺失，测评场景高风险 |
| **版本管理** | 规划 | 已实现（active/superseded 标记 + 4次 Alembic 迁移） | B 已具备基础版本管理 |
| **质量监控** | 规划阶段 | 66题测试集 + CI 每日回归 + 3轮 CC 过拒审查 | B 在检索质量监控上更成熟 |
| **审计追踪** | 规划独立审计运营层 | **无独立审计日志** | B 缺失 |

### 1.5 安全与权限对比

| 安全维度 | 参考方案（A） | kb2-web（B） | 风险等级 |
|---------|-------------|-------------|---------|
| **认证机制** | 未详述 | JWT 双通道 | B 可用 |
| **权限粒度** | 规划文档/段落级 | **admin/viewer 两级**，仅2用户 | B 粗粒度 |
| **检索前过滤** | 明确设计为强制环节 | **未实现**（检索在权限判断之前） | B 存在越权检索风险 |
| **脱敏机制** | 规划 | **未实现** | B 高风险（测评数据含敏感信息） |
| **审计日志** | 规划独立层 | **未实现** | B 缺失，不可追溯 |
| **内容审批** | 规划 | 代码层面有 CC 过拒审查逻辑，但**无审批工作流** | B 部分覆盖 |

### 1.6 评估验证体系对比

| 维度 | 参考方案（A） | kb2-web（B） |
|-----|-------------|-------------|
| **测试集** | 未提及 | **66题 LLM-Judge 测试集**，覆盖测评业务场景 |
| **CI/CD** | 未提及 | CI **每日回归** |
| **质量门禁** | 规划定期评估 | **3轮 CC 过拒审查** + T0/T1/T2 **三级精度调优** |
| **自动化评测** | 未涉及 | LLM-Judge 自动评分 |
| **测试代码量** | 未提及 | **36个测试文件** |
| **项目规模** | 设计文档 | 后端 **14,006行** + 前端 **33文件** Vue3+TS |

---

## 二、实践参考——kb2-web 可借鉴参考方案的要点

### 2.1 安全合规方向（高风险，需优先跟进）

| 借鉴要点 | 当前状态 | 建议动作 | 工时估计 |
|---------|---------|---------|---------|
| **数据脱敏** | 未实现 | 在文档解析入库前增加脱敏管道：身份证号、IP地址、系统名称、漏洞详情等正则匹配 + 可配置脱敏规则表 | **5 PD** |
| **内容级权限** | 仅 admin/viewer 两级角色 | 引入 document → role 映射表，在 RAG 检索后、LLM 生成前进行结果过滤 | **3 PD** |
| **审批工作流** | 概念矛盾标记（2,029条）无处置流程 | 为 contradictions 表添加状态字段（pending/reviewed/resolved），建立轻量审批 API | **2 PD** |
| **拒答策略** | 无安全边界 | 在 prompt 层增加安全护栏：识别越权问题、非测评领域问题、恶意提示注入 | **2 PD** |
| **审计日志** | 缺失 | 为 /query 端点增加请求日志表：user_id, query, timestamp, response_sources, tokens_used | **1 PD** |

### 2.2 数据治理方向

| 借鉴要点 | 当前状态 | 建议动作 | 工时估计 |
|---------|---------|---------|---------|
| **数据分级** | concept 表无敏感性标记 | 为 documents 和 concepts 表增加 sensitivity_level 字段 | **3 PD** |
| **数据补齐** | 3,809 concepts 中 93.5% 有 summary，仍有 6.5% 缺失 | 批处理补齐缺失 summary，建立补齐状态监控 | **1 PD** |
| **质量仪表盘** | 有测试集但无可视化质量趋势 | 参考方案A的审计运营层，构建轻量 Dashboard | **3 PD** |

### 2.3 架构扩展方向

| 借鉴要点 | 当前状态 | 建议动作 | 工时估计 |
|---------|---------|---------|---------|
| **PG 迁移** | SQLite 45MB，单文件部署简单但并发受限 | 当并发用户 > 10 或数据量 > 500MB 时，迁移至 PostgreSQL + pgvector | **10 PD** |
| **内网部署** | 当前依赖外网 DeepSeek API | 设计本地模型适配层（Ollama/vLLM 接口统一） | **5 PD** |
| **文档解析增强** | 当前解析器覆盖面未知 | 增强 PDF 表格、扫描件 OCR 支持 | **8 PD** |

---

## 三、优化提升建议

### P0（紧急，建议1-2周内完成）

| 编号 | 建议项 | 说明 | 工时 | 验收标准 |
|-----|-------|-----|-----|---------|
| **P0-1** | 数据脱敏管道 | 测评文档入库前必须脱敏，这是等保场景的合规底线 | **5 PD** | 使用含敏感信息的测试文档入库后，检索结果中不出现明文身份证号、IP地址、系统名称；通过 10 条脱敏测试用例 |
| **P0-2** | 检索后权限过滤 | 当前架构下 viewer 可检索到 admin 级文档内容 | **3 PD** | 为3份测试文档分别标记敏感度，viewer 角色仅返回可访问结果；通过 6 条权限测试用例 |
| **P0-3** | 审计日志 | 等保测评相关查询必须可追溯 | **1 PD** | 每次 /query 请求记录至 audit_log 表 |

### P1（重要，建议1-3个月内完成）

| 编号 | 建议项 | 说明 | 工时 |
|-----|-------|-----|-----|
| **P1-1** | 拒答安全护栏 | 防止模型在非授权领域或恶意提示下产生有害输出 | **2 PD** |
| **P1-2** | 概念矛盾审批流程 | 2,029 条 contradictions 需有人工确认与处置闭环 | **2 PD** |
| **P1-3** | 缺失 summary 补齐 | 约 **247条** 无 summary | **1 PD** |
| **P1-4** | 内容分级标记 | 为后续细粒度权限做准备 | **3 PD** |
| **P1-5** | PG 迁移评估 | 当前 SQLite 在多用户场景下存在写锁瓶颈 | **2 PD**（评估仅） |

### P2（长期，建议3-6个月内规划）

| 编号 | 建议项 | 说明 | 工时 |
|-----|-------|-----|-----|
| **P2-1** | 内网模型适配 | 支持切换至本地/内网部署的大模型，满足完全离线场景 | **5 PD** |
| **P2-2** | 质量运营仪表盘 | 为审计运营层构建可视化能力 | **3 PD** |
| **P2-3** | 文档解析增强 | 支持 PDF 表格提取、扫描件 OCR | **8 PD** |
| **P2-4** | 大规模知识平台准备 | 参考方案A第四阶段 | 需专项评估 |

---

## 附录A：kb2-web 核心数据快照

| 指标 | 数值 |
|-----|------|
| 数据库文件 | SQLite 45MB |
| Hindsight Bank 数 | 12 |
| 文档总数 | 303（285 active + 18 superseded） |
| Chunk 总量 | ~29,297 |
| Parent Chunks | 4,383 |
| Concepts | 3,809（summary 覆盖率 93.5%） |
| Concept Contradictions | 2,029 |
| KG Triples | 45 |
| Synonym Map | 158条 |
| Query Cache | 4条 |
| 用户数 | 2（1 admin + 1 viewer） |
| 后端代码量 | 56文件 / 14,006行 Python |
| 前端代码量 | 33文件 Vue3+TS |
| 测试文件 | 36 |
| 数据库迁移 | 4次 Alembic |
| 测试集规模 | 66题 LLM-Judge |
| Rerank 模式 | 5种（含 cross-encoder ~0.3s/query） |
| 召回通道 | 3路（语义 + BM25 + SQL） |
| 服务状态 | systemd active, :3027, uvicorn PID 1560539 |

## 附录B：术语对照表

| 方案A 术语 | 方案B 术语 | 说明 |
|-----------|-----------|------|
| pgvector | Hindsight | 向量存储引擎 |
| PostgreSQL FTS | BM25 | 全文检索实现 |
| 知识加工层 | chunk + concept + KG 抽取 | 知识结构 |
| 权限控制层 | JWT + role 感知 | 权限方案 |
| 审计运营层 | 测试集 + CI 回归 | 审计能力 |
| 可选 Reranker | 5种 rerank 模式 | 重排序 |
| 数据治理层 | 无独立模块 | 治理能力 |
